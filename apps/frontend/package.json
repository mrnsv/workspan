{
  "name": "workspan-frontend",
  "version": "1.0.0",
  "scripts": {
    "ng": "npx ng",
    "start": "npx ng serve",
    "build": "npx ng build",
    "watch": "npx ng build --watch --configuration development",
    "test": "npx ng test",
    "serve": "npx ng serve --host 0.0.0.0 --port 4200"
  }
}
